import type { NextPage } from "next";
import Logo from "../components/common/Logo";

const Home: NextPage = () => {
    return (
        <>
            <Logo />
        </>
    );
};

export default Home;
